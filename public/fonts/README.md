# Fonts for PDF certificates

Place these files here (download from Google Fonts or a reliable source):

- **Roboto-Regular.ttf** – required for certificate text
- **Roboto-Bold.ttf** – required for bold headings on the certificate

The certificate template loads them from `/fonts/Roboto-Regular.ttf` and `/fonts/Roboto-Bold.ttf`.
